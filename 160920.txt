인자값 저장된거 print하기

1번째 코드
(cmd 에서 실행됨)
#include <stdio.h>
#include <stdlib.h>  // exit()

int main(int argc, char* argv[]) {


  // 옵션 지정하지 않았을 때 에러 출력하고 종료
  if (argc == 1) {
    fputs("에러! 옵션을 입력하지 않으셨군요...\n", stderr);
    exit(1);
  }


  // 옵션 개수 출력
  printf("%d 개의 옵션을 입력하셨군요\n\n", argc - 1);


  // 옵션 배열의 요소들을 하나씩 출력
  for (int i = 1; i < argc; i++)
    printf("argv[%d] = %s\n", i, argv[i]);

  return 0;
}

---------------------------------------------------------
2번째 코드
(cmd에서 정상 실행됨)
#include <stdio.h>

int main(int argc, char* argv[])
{
	int i = 0;

	printf("argc = %d\n", argc);

	for (i = 0; i < argc; i++)
	{
		printf("argv[%d] = \n%s\n", i, argv[i]);
	}
}
--------------------------------------------------------


cmd 창에서 깨지는 한자 찾기
일본어 깨짐

來 , 歓, 一,  総, 数, 経


중국어

选, 单, 欢, 來, 逻, 辑, 盘, 区 总 数, 节, 择, 称, 号, 电, 软, 盘

---------------------------------------------------------
진척
1.메인소스에다가 인자값 함수 대입시켜보고 실행해보고 인자값 함수 맨밑에 첨부후 메인함수에 넣음


하지만 어떤식으로 응용해야 될지 모르겠음.

메인함수에 원래 소스를 집어넣어도 보았고, 함수 한개 더 만들어서 불러오기도 해봄






